# The public admin dashboard is disabled. Set this only when running the legacy
# chat server's protected maintenance endpoints during local development.
ADMIN_PASSWORD=

# Optional legacy realtime server URL used by the current Phase 1 application.
NEXT_PUBLIC_WS_URL=
